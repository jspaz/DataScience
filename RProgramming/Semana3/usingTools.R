# traceback, muestra el error más reciente
mean(x)
traceback()
# debug, evalúa paso a paso la función deseada
debug(lm)
lm(y -x)
# Para avanzar a la siguiente línea se escribe n y se presiona enter
# recover, muestra el menú callstack para ir directamente a la sección
options(error = recover)
read.csv("archivo")